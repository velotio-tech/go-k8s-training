apiVersion: v1
kind: Pod
metadata:
        name: nginx
        labels:
                app: velotio
spec:
        containers:
        - name: nginx
          image: nginx
          ports:
          - containerPort: 80
