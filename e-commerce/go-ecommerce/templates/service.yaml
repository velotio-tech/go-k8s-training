apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-ecommerce.fullname" . }}
  labels:
    {{- include "go-ecommerce.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "go-ecommerce.selectorLabels" . | nindent 4 }}
