version: "3.1"
services:
  db:
    image: postgres:14
    restart: always
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    volumes:
      - ./database/data:/var/lib/postgresql/data
    ports:
      - "5433:5432"
